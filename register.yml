server:
  port: 9005
eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: htto://${eureka.instance.hostname}:${server.port}
