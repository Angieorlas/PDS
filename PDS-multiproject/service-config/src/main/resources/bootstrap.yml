spring:
  application:
    name: service-config
  cloud:
    config:
      server:
        git:
          uri: file:///C:/PDS/PDS-multiproject/config-data/
  security:
    user:
      name: admin
      password: admin
server:
  port: 9098