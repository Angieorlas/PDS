spring:
  application:
    name: service-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Angieorlas/PDS.git
  security:
    user:
      name: admin
      password: admin
server:
  port: 9098